%hr{:style=>"width:400px;color:green;"}
%br
%br
=text_area_tag "use_comment_#{use.id}","<< Add comment",:size=>"50",:style=>"text-align:right"
%br
%br
%div{:id=>"comment_show_#{use.id}"}
 -comments=use.comments.find :all
 -if !comments.empty?
  -comments.each do |comment|
   =text_area_tag :use_comment,comment.title,:size=>"50",:style=>"text-align:right",:readonly => true
   -if @user.privilige=="Admin"
    =link_to "Delete",delete_comment_usecase_url(comment.id)
   -else
    =link_to "Delete",delete_comment_usecase_url(comment.id),:href=>"#"
   %br
   %br

